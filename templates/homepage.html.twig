{# Opnemen van de "Main" file #}
{% extends "main.html.twig" %}
{# nu de block content op in de main pagina vullen #}
{% block content %}
    {# Dit is commentaar #}

    {# Print #}
    <h4>Dit is de template {{  title }}</h4>

    {# ==================== START CAROUSEL HOME PAGE ====================== #}
    <div class="row">
        <div class="col-12">
            <div class="carousel slide carousel-home-page" id="carousel-370274">
                <ol class="carousel-indicators">
                    <li data-slide-to="0" data-target="#carousel-370274" class="active">
                    </li>
                    <li data-slide-to="1" data-target="#carousel-370274">
                    </li>
                    <li data-slide-to="2" data-target="#carousel-370274">
                    </li>
                </ol>
                <div class="carousel-inner">
                    <div class="carousel-item active">
                        <img class="d-block w-100" alt="Carousel Bootstrap First" src="https://www.layoutit.com/img/sports-q-c-1600-500-1.jpg" />
                    </div>
                    <div class="carousel-item">
                        <img class="d-block w-100" alt="Carousel Bootstrap First" src="https://www.layoutit.com/img/sports-q-c-1600-500-1.jpg" />
                    </div>
                    <div class="carousel-item">
                        <img class="d-block w-100" alt="Carousel Bootstrap First" src="https://www.layoutit.com/img/sports-q-c-1600-500-1.jpg" />
                    </div>
                    <a class="carousel-control-prev" href="#carousel-370274" data-slide="prev"><span class="carousel-control-prev-icon"></span> <span class="sr-only">Previous</span></a> <a class="carousel-control-next" href="#carousel-370274" data-slide="next"><span class="carousel-control-next-icon"></span> <span class="sr-only">Next</span></a>
                </div>
            </div>
        </div>
    </div>
    {# ==================== END CAROUSEL HOME PAGE ====================== #}

    <div class="row">
        <div class="col-4">
            <h1> Agenda </h1>
        </div>
        <div class="col-4">
            <div class="row">
                <div class="col-12">
                    <img src="#" />
                </div>
            </div>
            <div class="row">
                <div class="col-8">
                    <h3>Title</h3>
                </div>
                <div class="col-4">
                    <p>rating</p>
                </div>
            </div>
            <div class="row">
                <div class="col-12">
                    <p>short description</p>
                </div>
            </div>
            <div class="row">
                <div class="col-4">
                    <button>SMULLEN</button>
                </div>
                <div class="col=8">
                    <div class="row">
                        <div class="col-md-4">
                        Personen
                        </div>
                        <div class="col-md-4">
                        Prijs
                        </div>
                        <div class="col-md-4">
                        Calo
                        </div>
                    </div>
                </div>
            </div>
        </div>

    </div>

    {# Loop #}
    <ul>
    {% for record in data %}
        <li>
            {{ record.title }}
            {# sub-loop #}
            <ul>
                {% for ingr in record.ingredient %}
                  <li>
                      <span {% if ingr.ID < 2 %}style='color: #F00'{% endif %}>{{  ingr.name }}</span>
                      {# if #}
                      {%  if ingr.ID == 3 %}
                          <strong>Dit is ID 3!</strong>
                      {%  endif  %}
                  </li>
                {% endfor %}
            </ul>
        </li>
    {% endfor %}
    <ul>

    {# Dump #}
    <pre>
        {{ dump(data) }}
    </pre>

{% endblock %}
